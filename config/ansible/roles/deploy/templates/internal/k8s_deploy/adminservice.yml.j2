---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: 'app-{{ stage }}-{{ project_name }}-admin-service'
  namespace: '{{ k8s_namespace }}'
  annotations:
    reloader.stakater.com/auto: 'true'
spec:
  selector:
    matchLabels:
      app: 'app-{{ stage }}-{{ project_name }}-admin-service'
  template:
    metadata:
      labels:
        app: 'app-{{ stage }}-{{ project_name }}-admin-service'
    spec:
      imagePullSecrets:
        - name: secret-{{ project_name }}
      containers:
        - image: {{ image_name }}/{{ stage }}/admin-service:{{ version }}
          name: 'app-{{ stage }}-{{ project_name }}-admin-service'
          imagePullPolicy: Always
          workingDir: /var/www
          ports:
            - containerPort: 80
          volumeMounts:
          - name: tz-config
            mountPath: /etc/localtime
          - name: configmap-{{ stage }}-{{ project_name }}-env
            mountPath: /var/www/.env
            subPath: admin-service-env
          - name: configmap-{{ stage }}-{{ project_name }}-env-dusk-local
            mountPath: /var/www/.env.dusk.local
            subPath: admin-service-env-dusk-local
          - name: 'vol-{{ stage }}-{{ project_name }}'
            mountPath: /var/www/storage/education_material
            subPath: 'education_material'
          - name: 'vol-{{ stage }}-{{ project_name }}'
            mountPath: /var/www/storage/exercise
            subPath: 'exercise'
          - name: 'vol-{{ stage }}-{{ project_name }}'
            mountPath: /var/www/storage/exports
            subPath: 'exports'
          - name: 'vol-{{ stage }}-{{ project_name }}'
            mountPath: /var/www/storage/file
            subPath: 'file'
          - name: 'vol-{{ stage }}-{{ project_name }}'
            mountPath: /var/www/storage/questionnaire
            subPath: 'questionnaire'
          - name: 'vol-{{ stage }}-{{ project_name }}'
            mountPath: /var/www/storage/static_page
            subPath: 'static_page'
      volumes:
      - name: tz-config
        hostPath:
          path: {{ tz_path }}
      - name: configmap-{{ stage }}-{{ project_name }}-env
        configMap:
          name: configmap-{{ stage }}-{{ project_name }}-admin-service-env
      - name: configmap-{{ stage }}-{{ project_name }}-env-dusk-local
        configMap:
          name: configmap-{{ stage }}-{{ project_name }}-admin-service-env-dusk-local
      - name: 'vol-{{ stage }}-{{ project_name }}'
        persistentVolumeClaim:
          claimName: 'pvc-{{ stage }}-{{ project_id }}-admin-service'
---
apiVersion: v1
kind: Service
metadata:
  name: 'svc-{{ stage }}-{{ project_name }}-admin-service'
  namespace: '{{ k8s_namespace }}'
spec:
  ports:
    - port: 80
  selector:
    app: 'app-{{ stage }}-{{ project_name }}-admin-service'
